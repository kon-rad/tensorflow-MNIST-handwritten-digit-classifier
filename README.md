# tensorflow-MNIST-handwritten-digit-classifier
This is a handwritten digit image classifier using a logistic regression model. It trains on the MNIST data set and then tests the trained model on it.
